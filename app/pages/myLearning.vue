<script setup lang="ts">
const color = ref("red");
</script>
<template>
  <div>
    Страница для использования фишек
    <img src="~/assets/image/photo.jpg" alt="Discover Nuxt" />
    <p :class="$style.red">This should be red</p>
    https://github.com/nuxt-modules/fontaine Когда стоит установить Nuxt
    Fontaine: Вы стремитесь к улучшению Core Web Vitals, особенно уменьшению
    CLS. Веб-страницы с низким CLS лучше ранжируются в поисковых системах и
    обеспечивают более приятный пользовательский опыт. Ваш сайт содержит шрифты,
    которые вызывают сдвиги макета. Если вы замечаете, что шрифты «скачут» или
    элементы перемещаются при загрузке, Nuxt Fontaine поможет устранить эти
    проблемы. Вы используете или планируете использовать кастомные локальные
    шрифты. В таком случае Nuxt Fontaine сможет обеспечить более плавную и
    предсказуемую загрузку. Проект уже настроен на оптимизацию скорости — и вы
    хотите максимально снизить CLS. Когда може быть не особенно нужно: Если ваш
    сайт уже идеально оптимизирован, и CLS низкий без дополнительных усилий,
    установка может быть излишней. Если он использует только системные шрифты
    или очень простые стили — выгоды минимальны.
  </div>

  <NuxtLink to="/about">About</NuxtLink>

  <!-- <script setup>
import { definePageMeta } from '#imports'

// Определяем метаданные страницы
definePageMeta({
  title: 'Личный кабинет',
  // Можно указать произвольные свойства
  meta: [
    { hid: 'description', name: 'description', content: 'Страница личного кабинета' }
  ],
  // Используем validate для проверки
  validate: (route, navigate) => {
    const user = useAuthUser(); // гипотетическая функция авторизации
    if (!user.value.isLoggedIn) {
      // Перенаправляем на страницу входа, если не авторизован
      navigate('/login');
      return false; // отмена перехода на страницу
    }
    return true; // можно попасть
  }
});
</script>

<template>
  <div>
    <h1>Добро пожаловать в личный кабинет</h1>
  </div>
</template>
Что делает этот код?
В definePageMeta() задаются свойства страницы (заголовок, мета-теги).
В validate() — проверка, авторизован ли пользователь.
Если нет — происходит редирект и страница не загружается.
Если да — страница показывается. -->
</template>

<style scoped lang="scss">
/* v-bind можно использовать сразу в стилях! */
.text {
  color: v-bind(color);
}
</style>

<style module lang="scss">
.red {
  color: red;
}
</style>
